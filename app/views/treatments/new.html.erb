<h1 class="mb-4">New Treatment</h1>
<p class="text-muted">For appointment on <%= @appointment.date.strftime("%B %d, %Y") %></p>
<%= render "form" %>
<%= link_to "← Back to Appointment", @appointment, class: "btn btn-secondary mt-2" %>