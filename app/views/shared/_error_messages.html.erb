<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h6><strong><%= pluralize(object.errors.count, "error") %> prevented this record from being saved:</strong></h6>
    <ul class="mb-0">
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>